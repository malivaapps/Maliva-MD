<!-- res/drawable/autocomplete_background_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <stroke
                android:width="1dp"
                android:color="@color/primary" />
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <!-- Change the stroke color for the unfocused state -->
            <stroke
                android:width="1dp"
                android:color="@color/text_secondary_lighter" />
            <corners android:radius="8dp" />
        </shape>
    </item>
</selector>
